Installation instructions

0.You should have webpack, node and nodemon(allows hot reload) installed globally to run the package. After you install them run these commands:

1.npm install
2.npm run serve

App will be availiable at url http://localhost:3000/
Tested on Linux Mint(Chromium) and on Windows 10(Chrome)